1) Follow directions in config.sql (in root of the git repo) to start up the sql server 
2) Run npm install to get all the dependencies 
3) Run npm start to start the app server (located at 127.0.0.1:3000
